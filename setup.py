#!/usr/bin/env python

from distutils.core import setup

#INSTALL_REQUIRES = ['xarray >= 0.10.6', ]

setup(name='dashydro',
      description='DAS hydrology',
      url='https://github.com/apatlpo/das_work',
      packages=['dashydro'])

#      install_requires=INSTALL_REQUIRES,
